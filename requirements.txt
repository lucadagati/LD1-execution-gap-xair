# Runtime dependencies (mirrors [project.dependencies] in pyproject.toml).
# Exact versions used for the reported campaign: requirements.lock
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
jsonschema>=4.21.0
redis>=5.0.0
websockets>=12.0
